/users/mo/learngit
这是第一次修改，第一行为第一次用vim创建时所写的内容（文件所在路径）。
作为第一次修改，mo打算写入这两行中文，英文一起的内容。

git is a distributed version control system.
git is free software.

git has a mutable index called stage

git tracks changs of files.

推送到远程github
